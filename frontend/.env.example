# Leave empty to use the Vite proxy to http://127.0.0.1:8081
VITE_API_BASE_URL=

# On Render this is unnecessary: the Blueprint passes the API's public hostname as
# API_HOST and vite.config.ts derives the base URL from it. Set VITE_API_BASE_URL only
# to point a build somewhere else by hand.
